import type { Show } from '@/lib/data';
export default function ShowCard({show}:{show:Show}){return <article className={`showCard ${show.tone}`}><div className="showVisual" style={show.poster?{backgroundImage:`linear-gradient(180deg,rgba(0,0,0,.08),rgba(0,0,0,.72)),url(${show.poster})`,backgroundSize:'cover',backgroundPosition:'center'}:undefined}><span>{show.genre}</span><b>{show.artist}</b></div><div className="showBody"><small>{show.date} · {show.time}</small><h3>{show.title}</h3><p>{show.venue}</p><div className="tags">{show.tags.map(t=><em key={t}>{t}</em>)}</div></div></article>}
