export default function Footer() {
  return (
    <footer className="border-t border-line py-10">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-2 px-6 text-xs text-muted sm:flex-row sm:items-center sm:justify-between">
        <p>SHOWDAY — 하루애(㈜미니멈)</p>
        <p>공연 데이터 제공: 공연예술통합전산망(KOPIS)</p>
      </div>
    </footer>
  );
}
